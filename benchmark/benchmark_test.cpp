#include <atomic>
#include <iostream>
#include <memory>
#include <string>
#include <thread>
using namespace std;

int main(int argc, char *argv[]) {
    int iterations = 100000;
    int thread_size = 10;
    int queue_size = 5;
}